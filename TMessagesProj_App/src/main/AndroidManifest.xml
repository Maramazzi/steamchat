<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:installLocation="auto">

    <!-- GoogleVoiceClientActivity/Service (Google Assistant "send a message via Telegram" voice
         action) removed: zero internal references, and it's another Telegram-branded entry point
         SteamChat has no business exposing. -->
    <application android:name="org.telegram.messenger.ApplicationLoaderImpl" tools:replace="name">
    </application>
</manifest>